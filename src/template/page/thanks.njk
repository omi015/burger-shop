{% extends "index.njk" %}

{% block body %}

    <section>
        <div class="flex flex-col justify-center items-center h-[30vw]">
            <div>
                <h3 class="text-black text-4xl font-bold container">gracias
                    {{User.name}}
                    por comprar</h3>
                <div class="text-center">
                    <span class="">direccion:
                    </span>
                    <b>{{address}}</b>

                </div>

            </div>

        </div>

    </section>

{% endblock %}